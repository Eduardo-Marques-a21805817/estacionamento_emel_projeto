import 'package:flutter/material.dart';

class RegisterIncidentPage extends StatelessWidget {
  const RegisterIncidentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}